{
  "name": "iurispocket",
  "version": "1.0.0",
  "description": "Biblioteca Jurídica de Panamá — App Android offline",
  "scripts": {
    "build-apk": "npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
